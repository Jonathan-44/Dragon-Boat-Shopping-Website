<h1>New Dragon Race</h1>

<%= form_with(model: @dragon_race, local: true) do |form| %>
  <% if @dragon_race.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dragon_race.errors.count, "error") %> prohibited this dragon race from being saved:</h2>

      <ul>
        <% @dragon_race.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', dragon_races_path %>
